<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Success" Height="180" Width="350" Background="{DynamicResource PanelBg}" WindowStartupLocation="CenterScreen" FontFamily="Segoe UI" ResizeMode="NoResize">
    <Grid Margin="25">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Foreground="{DynamicResource PrimaryText}" FontSize="14" TextWrapping="Wrap" VerticalAlignment="Center" Text="Configuration saved successfully."/>
        <Button x:Name="btnOk" Grid.Row="1" Content="OK" HorizontalAlignment="Right" Background="#007ACC" Foreground="#FFFFFF" Padding="25,8" BorderThickness="0" Cursor="Hand">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="6">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="15,6"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>